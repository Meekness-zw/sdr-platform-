services:
  - type: web
    name: sdr-platform
    env: python
    pythonVersion: 3.13.12
    plan: free
    region: oregon
    buildCommand: CARGO_HOME=/tmp/.cargo pip install --prefer-binary -r requirements.txt
    startCommand: python main.py
